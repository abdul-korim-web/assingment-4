## 🌦️ Job Application Tracker

🧩 Overview

This is my assingment-4 repo for programing hero.


## 🚀 Live Demo

Experience the app live: [Job Application Tracker](https://abdul-korim-web.github.io/assingment-4/)


# Answers to Questions 
##  1
এটা দিয়ে HTML এর মধ্যে নির্দিষ্ট একটা id ব্যবহার করে একটি element খুঁজে পাওয়া যায়। যেহেতু id সাধারণত একটাই থাকে, তাই এটা শুধু একটা element return করে।

getElementsByClassName:
এটা দিয়ে একই class name থাকা অনেকগুলো element একসাথে পাওয়া যায়। এটা একাধিক element return করে।

querySelector:
এটা CSS selector ব্যবহার করে element খুঁজে বের করে। তবে যদি অনেকগুলো element থাকে, তাহলে এটা শুধু প্রথমটা return করে।

querySelectorAll:
এটাও CSS selector ব্যবহার করে element খুঁজে, কিন্তু এখানে যতগুলো matching element থাকে সবগুলোই return করে।

## 2
নতুন element তৈরি করতে JavaScript এ document.createElement ব্যবহার করা হয়।

তারপর সেই element কে DOM এ ঢোকাতে appendChild বা append ব্যবহার করা হয়।
## 3
Event Bubbling হলো এমন একটা system যেখানে কোনো element এ event ঘটলে সেই event ধাপে ধাপে তার parent element গুলোর দিকে উপরে উঠতে থাকে।
